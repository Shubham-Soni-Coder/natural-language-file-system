#Start_router.py 

Purpose:
Handle the startup scan operation and store scanned file metadata in the database.
    
Trigger:
Application Startup Event

Flow:
Server Start
↓
Create Database Session
↓
Create FileUtils Object
↓
scan()
↓
FileService.ingest()
↓
Save Metadata into Database
↓
Finish 

Output:
File metadata into Database

#Tools_router.py

Purpose:
Retrive all available tools registered in the MCP Registry.

EndPoint 
GET /tools

Input:
- Mcp Registry Dependency 

Flow:
CLient Request 
↓
get_tools()
↓
mcp.get_tools()
↓
Retrive Registered Tools
↓
Return Tool List 
↓
Finish 

Output:
{
    "status":"Success",
    "tools":[...]
}

#execute_tool.py

Purpose:
Execute a tools from the Mcp Registry using the provided tool name and arguments.

Endpoint:
POST /execute

Input:
- tool_name
- tool_arguments
- MCP Registry Dependency

Flow:
Client Request
↓
execute_tool()
↓
mcp.execute_tool(
    tool_name,
    arguments
)
↓
Find tool in Registry
↓
Execute tool
↓
Get Result
↓
Return Response

Output:
{
    "status":"success",
    "data",result
}


# query_router.py

Purpose:
Handle natural language user queries and execute the selected tools.

Endpoint:
POST /query

Input:
- query : str

Dependencies:
- QueryService
- AiDriver
- MCPRegistry
- Database Session

Flow:

User Query
    ↓
process_query()
    ↓
QueryService.process_query()
    ↓
mcp_registry.get_tools()
    ↓
Get Available Tools
    ↓
ai_handler.run_ai(query, tools)
    ↓
AI Selects Tool(s)
    ↓
Extract tool_name and arguments
    ↓
Validate tool exists in MCPRegistry
    ↓
mcp_registry.execute_tool()
    ↓
Execute Selected Tool
    ↓
Collect Results
    ↓
Return Response

Output:
[
    {
        "tool": tool_name,
        "result": result
    }
]

Possible Errors:
- 400 : Could not understand query
- Tool not found
- Tool execution failed

#server_router.py

Purpose:
Check the database connection status.

Endpoint:
Get /database

Input:
- Database Session

Flow:

Client Request
↓
read_db()
↓
Execute SELECT 1
↓
Check Query Success
↓
Return Database Status

Output:
{
    "connection":true,
    "message":"Db connected"
}

#user_router.py

Purpose:
Create and retrives user records from the database.

Endpoint:
POST /user
GET /user

Post /user:
Input:
- name
- email
- Database Session

Flow:
Client Request
↓
Validation UserCreate Schema
↓
Create User Object
↓
Save data into Database
↓
Commit change
↓
Refresh user
↓
Return user

Output:
UserResponse

Get /user
Input:
- Database Session

FLow:
Client Request
↓
get_user()
↓
Query User Table
↓
Get all Users
↓
Return User list

Output:
List[User] 

#file_router.py

Purpose:
Create and retrieve file records from the database using FileServie.

Endpoints:
GET /files
POST /files
POST /user/{user_id}/files

GET /files
Input:
- Database Session

Flow:
Client Request
↓
get_files()
↓
FileServie.get_all()
↓
Query Database
↓
Get all Files
↓
Return Files
↓
Finish

POST /files
Input:
- request : FIleCreate
- Database Session

Flow:
Client Request
↓
POST /files
↓
create_files()
↓
Validation FileCreate Schema
↓
FileService.create()
↓
Create File Record
↓
Save to database
↓
return FileResponse
↓
Finish

Output:
List[files]

POST /user/{user_id}/files
Input:
- user_id : int 
- Database Session

Flow:
CLient Request
↓
Post /user/{user_id}/files
↓
get_user_files()
↓
Query Database
↓
Filter by user_id
↓
Get user data
↓
Return files
↓
Finish

Output:
List[files]

#task_roter.py

Purpose: 
    Create and retrice task records from the dataset using TaskService

Endpoints:
POST /tasks

Input:
DatabaseSession
TaskCreate

Flow:
Client Request
↓
Post /tasks
↓
create_task
↓
Validation TaskResponse schema
↓
TaskService.create()
↓
Create Task object
↓
Save to database
↓
return TaskRespone
↓
Finish

Output:
List[Task]

#execute_router.py

Purpose:
handle database integrity errors and return user_friendly error message.

Trigger:
Integrity Exception

Flow:

Application Start
↓
add_exception_handlers(app)
↓
Register Integrity Handler
↓
Wait for Exception
↓
IntegrityError Raised 
↓
log Technical error
↓
Create JsonResponse
↓
Return HTTP 400 Response
↓
Finish

Output:
{
    "error": "Database Error",
    "detail": "This record (e.g., email or filename) already exists in our system."
}








